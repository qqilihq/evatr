# Regenerated by `pnpm run scrape-error-codes`, which writes `JSON.stringify`
# output; formatting it here would be undone on the next scrape.
lib/error-codes.ts

# Comments and trailing commas that Prettier would rewrite
tsconfig.*

pnpm-lock.yaml
